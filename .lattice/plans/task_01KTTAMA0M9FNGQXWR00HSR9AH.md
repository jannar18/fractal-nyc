# FRAC-199 — Fix 7 pre-existing baseline test failures

## Scope
Repair 7 stale assertions across `buttons`, `footer`, `navigation`, `neighborhood`
test suites. Diagnosis: **all 7 are STALE TESTS (class A)** — the production code
is the intended current state (frosted button FRAC-52, `--btn-accent`→`--accent`
rename FRAC-215, masthead de-canvas FRAC-211, border-tier FRAC-215). No real
regressions. Fixes update assertions to match current correct behavior while
**preserving each test's original intent** (FRAC-86/88 regressions, abbreviation,
centering, viewport layout). No source/component edits.

## Per-failure diagnosis table

| # | Failure | Root cause | Class | Fix | Evidence |
|---|---------|-----------|-------|-----|----------|
| 1 | `buttons.test.tsx:77` "mark corner decorations as aria-hidden" — `expected 5 to be 4` | FRAC-52 frosted default variant adds a paper-grain `<span … aria-hidden>` (button.tsx:143) **in addition** to the 4 Mandelbrot corner spans (button.tsx:144-171). Total `[aria-hidden]` = 5. | **A stale-test** | Update expected `4` → `5`. Keep meaningful: `expect(corners.length).toBe(5);` Add comment: `// 4 Mandelbrot corners + 1 FRAC-52 paper-grain overlay`. Intent preserved (every decorative element hidden from AT). | button.tsx:141-173: grain span + 4 corner spans, each `aria-hidden`. Current code intended (FRAC-52). |
| 2 | `buttons.test.tsx:135` "asChild over `<a>` as styled CTA link" — className lacks `bg-[rgba(242,234,216,0.08)]` | Default variant restyled by FRAC-52 (frosted) + FRAC-215 (`--btn-accent`→`--accent`). Rest-state fill is now `bg-[var(--accent,currentColor)]`. The cream-rgba fill class no longer exists at rest (only inside the `hover:bg-[var(--btn-fill,rgba(242,234,216,0.16))]` fallback). | **A stale-test** | Replace stale fill assertion. Intent = "asChild `<a>` still gets frosted CTA chrome." Keep `inline-flex`, `max-w-xs`, `w-full`; change line 135 `toContain("bg-[rgba(242,234,216,0.08)]")` → `toContain("bg-[var(--accent,currentColor)]")`. Keep line 136 (`[border-color:var(--accent,currentColor)]`) — already matches current code. | button.tsx:51-59 (default variant). Line 136 assertion already passes against current code. |
| 3 | `footer.test.tsx:47` "branding text with italic styling" — `querySelector(".italic")` null | Footer branding wordmark is now a `<Link>` styled via **inline `style`** (`fontFamily: 'Jacquard 24'`, `letterSpacing`, `fontSize`) with NO `italic` class (Footer.tsx:83-97). `.italic` only exists on the Navbar "Collective" sub-wordmark, not the Footer. The Footer "Fractal" wordmark is not italic in current markup. | **A stale-test** | Intent (FRAC-88) = "branding is the styled 'Fractal' wordmark." Retarget selector to the actual branding Link: `container.querySelector('[aria-label="Fractal — back to home"]')` (stable, semantic). `expect(brandingEl).toBeTruthy()` + `expect(brandingEl!.textContent).toContain("Fractal")`. Drop `.italic` dependency (wordmark is Jacquard-styled, not italic — do NOT re-add italic to code). | Footer.tsx:83-97: `<Link aria-label="Fractal — back to home" style={{fontFamily:"'Jacquard 24'…"}}>Fractal</Link>`. No `italic`. |
| 4 | `footer.test.tsx:54` "Jacquard 24 font for branding" — `querySelector(".italic")` null | Same as #3 — `.italic` element does not exist. The Jacquard font is in the branding Link's inline `style` (Footer.tsx:89). | **A stale-test** | Retarget to same branding-Link selector as #3, then `expect(brandingEl!.getAttribute("style")).toContain("Jacquard 24")`. Preserves true intent (branding uses Jacquard 24) against current markup. | Footer.tsx:88-94: `style={{ fontFamily: "'Jacquard 24', system-ui", … }}`. |
| 5 | `navigation.test.tsx:166` "mobile nav abbreviated labels" — `querySelector(".lg\:hidden")` null | **Selector escaping bug**, not a markup change. In a JS double-quoted string `".lg\:hidden"` → `\:` collapses to `:`, yielding CSS `.lg:hidden` (invalid pseudo-class) → null. The mobile nav DOES render (Navbar.tsx:206 `<div className="lg:hidden …">`) with abbreviated letters (lines 247-251: `Education`→`E`, `Political Club`→`PC`, else `name[0]`). Sibling test line 157 correctly uses `".max-lg\\:hidden"`. | **A stale-test** (test bug) | Fix escape: `document.querySelector(".lg\\:hidden")`. Keep existing assertions (`.toContain("E")`, `.not.toContain("PC")`). See Ambiguity below re `PC`. | Navbar.tsx:206, 232-255. Escaping contrast vs line 157 (`.max-lg\\:hidden`). |
| 6 | `neighborhood.test.tsx:79` "center ordered list in note container" — `centeringDiv.querySelector("ol")` null | `querySelector(".flex.justify-center")` returns the **first** match. After FRAC-211 the Navbar mobile menu buttons (`flex items-center justify-center`) now appear before the note's centering div in document order, so the selector grabs a Navbar button (no `ol`). The note's true centering div (`flex justify-center mt-1.5 md:mt-3`, NeighborhoodPage.tsx:68) is the 3rd `.flex.justify-center`. **DOM dump confirmed:** 5 `.flex.justify-center` nodes; only the 3rd contains the `ol`. | **A stale-test** (selector too broad / brittle to Navbar refactor) | Scope selector to the note container: `container.querySelector(".max-w-xl .flex.justify-center")` (note wrapper is `max-w-xl mx-auto`, NeighborhoodPage.tsx:61). `expect(centeringDiv).toBeTruthy()` + `expect(centeringDiv!.querySelector("ol")).toBeTruthy()`. Preserves intent (ol wrapped in flex-centering div inside note). | NeighborhoodPage.tsx:61 (`max-w-xl mx-auto`), 68 (`flex justify-center` wrapping `<ol>`). DOM dump confirmed. |
| 7 | `neighborhood.test.tsx:131` "full-viewport-height layout" — `.min-h-screen.flex.flex-col.items-center.justify-center` null | Content section vertical alignment changed: NeighborhoodPage.tsx:32 is now `min-h-screen flex flex-col items-center **justify-start**` (top-aligned w/ `pt-16 md:pt-24`), not `justify-center`. So the compound selector (requires `justify-center`) misses. Page IS still full-viewport (`min-h-screen` on both `<main>` and content wrapper). | **A stale-test** | Intent = "full-viewport-height flex column layout." Update selector to current markup: `container.querySelector(".min-h-screen.flex.flex-col.items-center.justify-start")` (matches the exact current class set so it still verifies real layout). Keep assertion intent. | NeighborhoodPage.tsx:32: `relative z-10 min-h-screen flex flex-col items-center justify-start …`. |

## Classification summary
- **All 7 = (A) STALE TEST.** Production code reflects intentional this-session changes (FRAC-52 frosted button, FRAC-211 navbar de-canvas, FRAC-215 token rename + borders, FRAC-88 Footer wordmark design). No code changes — only test assertions updated, faithfully preserving each original intent. No assertions weakened: each fix re-targets the same property/structure against the new correct DOM.

## Ambiguity / human-judgment flags
- **#5 (`PC` assertion):** Code (Navbar.tsx:250) still maps `Political Club → "PC"`, but FRAC-161 removed Political Club from `visibleSectionLinks` (the array iterated at line 233). The `.not.toContain("PC")` assertion is correct **iff** Political Club is genuinely absent from `visibleSectionLinks`. The implementer MUST verify by rendering the fixed-selector node and inspecting `textContent` — if "PC" unexpectedly appears, the test caught a real divergence (Political Club not actually hidden) → escalate, do NOT weaken the assertion. Not blocking planning.
- No other genuine ambiguities. #1-#4, #6, #7 are mechanical retargets with unambiguous correct DOM.

## Acceptance criteria
1. `pnpm test` passes (0 failures; previously 7 failed / 148 passed → target 155 passed).
2. No edits to any file under `src/` outside `src/__tests__/`. (Test-only change.)
3. Each fixed assertion still verifies the same intent (no `toBeTruthy()`-only stubs, no deleted assertions).
4. #5 implementer confirms `PC` is genuinely absent before relying on `.not.toContain("PC")`.

## Key files
- Tests (edit): `src/__tests__/buttons.test.tsx`, `footer.test.tsx`, `navigation.test.tsx`, `neighborhood.test.tsx`
- Sources (reference only, DO NOT EDIT): `src/components/ui/button.tsx`, `src/components/layout/Footer.tsx`, `src/components/layout/Navbar.tsx`, `src/pages/NeighborhoodPage.tsx`, `src/components/ui/MandelbrotCorners.tsx`
